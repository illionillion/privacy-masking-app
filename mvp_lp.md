# 画像マスキングツール LP（MVP仕様書）

---

## 1. 目的

このLPの目的は以下の3点である。

- ユーザーに「何ができるサービスか」を3秒以内で理解させる
- 実際のツール利用へスムーズに誘導する
- 「画像を外部に送らない」という安心感を明確に伝える

---

## 2. 想定ユーザー

主な利用シーンおよびターゲットは以下の通り：

- SNS投稿前に写真の顔や個人情報を隠したいユーザー
- 社内資料やスクリーンショットを安全に共有したいユーザー
- イベント・オフ会などの写真を公開用に加工したいユーザー

---

## 3. コアメッセージ（1行）

LPの最上部に表示する主要メッセージ：

> ブラウザだけで画像の個人情報を安全に隠せるツール

または短縮版：

> 画像をアップロードせず、その場でマスキング

---

## 4. ページ構成

LPは以下のセクション構成とする。

### ① Heroセクション（最重要）
- サービス名
- キャッチコピー（1行）
- サブコピー（安心・特徴）
- CTAボタン：「今すぐ使う」

---

### ② デモセクション
- Before / After の比較表示
- 顔・PC画面・テキストが隠れている具体例
- 1枚で価値が伝わる構成

---

### ③ 使い方（3ステップ）
- 画像をドラッグ＆ドロップ
- 自動または手動でマスク処理
- ダウンロード

---

### ④ プライバシー説明
- 画像はサーバーに送信されない
- すべてブラウザ内で処理される
- ログイン不要で利用可能

---

### ⑤ ユースケース
- SNS投稿前の画像編集
- 社内資料・スクリーンショットの共有
- イベント写真の公開用加工

---

### ⑥ CTAセクション
- 「今すぐ使う」ボタン
- 再度ツールへ誘導

---

## 5. 必須UI要素

LPおよびツールに必要なUI要素：

- ドラッグ＆ドロップ領域
- 画像プレビューエリア
- マスク処理プレビュー（リアルタイム）
- ダウンロードボタン
- CTAボタン（複数配置可）

---

## 6. 技術制約

- Next.js（App Router構成）
- LPは `/lp` または `/` 配下に配置
- 静的生成でも動作可能な構成
- 画像処理は完全にクライアントサイドで実行
- サーバーへの画像送信は禁止

---

## 7. 使用ライブラリ方針（AI選定対象）

以下の機能は実装時にライブラリ選定をAIに委任する：

- Canvas操作
- 画像ドラッグ＆ドロップ処理
- 画像マスク処理（ぼかし・スタンプ・塗りつぶし）
- 状態管理（必要な場合のみ）
- UIコンポーネント（Tailwindベース）

---

## 8. 非機能要件

- 画像データをサーバーに送信しない設計
- 1ページで完結するシンプル構成
- 3秒以内で価値が理解できるUI設計
- モバイル対応（レスポンシブ必須）
- 軽量で高速な動作

---

## 9. KPI（簡易指標）

本LPの成功指標：

- LP滞在時間
- ツール利用開始率（CTAクリック率）
- 画像処理実行率（実際に使われた割合）

---

## 10. 将来拡張（MVP外）

将来的に追加可能な機能：

- 透かし機能（無料/有料差分）
- 有料プラン（広告非表示・高解像度出力）
- バッチ処理（複数画像対応）
- API提供（開発者向け）